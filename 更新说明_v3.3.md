# v3.3 缩略预览与大图查看版更新说明

## 图片列表升级

- 图片列表第一列直接显示 54×54 缩略预览图，不再依赖独立侧边预览区。
- 每行高度调整为 66px，页序、文件名、像素尺寸、大小和目录信息更易扫读。
- 缩略图保持原图比例，使用白色圆角底板和细边框，不拉伸图片。
- 鼠标悬停缩略图约 170ms 后显示大图浮层，自动根据屏幕空间选择左右位置。
- 缩略图右上角在悬停时显示放大镜按钮，点击即可进入大图查看器。
- 双击图片行直接打开大图查看器。
- 右键菜单增加查看大图、打开所在位置、上移、下移和删除。

## 大图查看器

- 支持上一张、下一张浏览，并显示当前序号、像素尺寸和文件大小。
- 支持鼠标滚轮缩放、按钮缩放、适应窗口和原始尺寸。
- 支持按住鼠标拖动画面。
- 支持方向键切换图片，Esc 关闭窗口，Ctrl+0 恢复原始尺寸。
- 支持一键打开当前图片所在目录。
- 透明图片在查看器内以白色背景合成显示，避免透明区域变黑。

## 排版与交互精修

- 步骤条连接线与文字彻底分层，解决文字被线条压住的问题。
- 文件统计标签加宽并补充文件/图片图标，长统计内容不再越界。
- 移除图片列表右侧常驻预览栏，释放更多文件名与路径显示空间。
- 图片合成 PPT 的保存位置移入固定操作区，常用设置在 1200×800 默认窗口内无需滚动。
- 设置区按快速预设、页面设置和图片布局分组，并补充统一的小图标与分隔线。
- 进度条升级为圆角轨道、百分比胶囊和处理中动态高光。
- 卡片继续统一为 12px 圆角，按钮、输入框、下拉框统一为 8px，小型图标按钮统一为 6px。

## 兼容与测试

- 保留 PPT/图片/文件夹拖拽导入、自动识别、去重和图片拖动排序。
- 保留 PowerPoint 优先、LibreOffice 备用的 PPT 导图策略。
- 已完成代码编译、三种比例图片缩略图加载、悬停浮层、大图查看器交互测试。
- 已完成三张图片生成 PPT，再通过 LibreOffice 导出为三张 PNG 的往返测试。
