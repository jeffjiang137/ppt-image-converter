# Ant Design 组件映射与交互规范

本工具仍使用 Python Tkinter 实现，但界面结构和交互规则按照 Ant Design 的企业级产品设计方法进行映射。

## 组件映射

| Ant Design 组件 | 桌面工具中的实现 |
|---|---|
| Layout / Sider | 左侧固定工作区导航 + 右侧内容区域 |
| Menu | 两个转换模式及明确选中态 |
| Breadcrumb | 顶部当前功能路径 |
| Steps | 导入、设置、转换三步流程 |
| Upload.Dragger | 支持点击与拖拽的虚线导入区域 |
| Table | 文件列表、列标题、选中态和空状态 |
| Card | 紧凑流程条、文件卡片、设置卡片、通知与记录窗口 |
| Form | 输出路径、格式、DPI、比例和布局等参数 |
| Select / InputNumber | 下拉选择和数值输入 |
| Radio Card / Segmented | 场景预设选择卡片 |
| Alert | 参数说明和风险提示 |
| Progress | 底部转换进度 |
| Message / Notification | 导入、排序、预设和转换结果反馈 |
| Empty | 没有文件时的空状态引导 |
| Tag / Badge | 文件数量和导出环境状态 |

## v3.2 组件细化

### 圆角卡片

- 页面主卡片统一使用约 12px 视觉圆角
- 输入、下拉、按钮和预设选择使用约 8px 圆角
- 小标签和复选框使用 4～6px 圆角
- 卡片边框使用浅灰色，搭配轻量下沉阴影，避免厚重悬浮感
- 卡片内部仍遵循 8 / 16 / 24 间距体系，不因圆角额外挤压内容

### Select 下拉选择

- 闭合态使用 8px 圆角、约 36px 控件高度
- 默认边框为中性灰；悬停时变为浅蓝；聚焦或展开时使用主色描边
- 展开时箭头方向反转，当前选项使用浅蓝底和勾选标识
- 选项悬停使用弱灰底，避免与当前选中项混淆
- 支持鼠标点击、Enter、Space、Down 和 Esc 操作

### 滚动条与进度条

- 删除上下箭头按钮，仅保留自绘圆头滑块
- 轨道与页面背景融合，滑块使用中性灰
- 悬停和拖动时逐级加深
- 内容未溢出时自动隐藏，减少无效视觉噪声
- 设置面板与文件列表统一使用同一套滚动条样式
- 进度条采用圆角轨道、动态主色填充和百分比反馈

### 信息密度

- 顶部标题是页面唯一的大标题，内容区不再重复同名标题
- 三步流程、文件数量集中在 56px 紧凑流程条中
- 处理记录从页面常驻卡片改为底部入口，按需打开
- 默认 1200×800 窗口中，常用参数无需滚动即可使用
- 小窗口优先保留导入、输出和开始按钮，预览等次要内容可自动收起

## 设计原则

### 自然

操作入口与用户意图一致：拖入文件即可导入，点击拖拽区即可选择文件，图片列表直接拖动即可调整页序。

### 确定

每个状态都有明确反馈：选中、悬停、禁用、执行中、完成、警告和失败均使用统一语义色和文案。

### 有意义

主界面只突出完成任务所需的信息，处理记录按需打开，图片预览在空间不足时自动收起。

### 成长

参数区域在小窗口下可滚动，后续增加更多输出格式或批处理规则时，不需要破坏现有布局。

## 间距与层级

- 8px：控件内部或紧密相关元素
- 16px：同一模块内的常规间距
- 24px：页面级模块间距
- 主标题 > 模块标题 > 表单标签 > 辅助说明
- 主操作仅保留一个高强调按钮，次要操作使用默认或文字按钮

## 防错规则

- 没有文件时禁止开始转换
- 没有输出路径时禁止开始转换
- 清空列表前二次确认
- 重复文件自动跳过
- 不支持的文件明确提示
- 任务执行时禁用新的开始操作

## v3.3 图片预览组件规范

- 图片列表使用 Treeview 的首列作为 54×54 缩略图区域，行高统一为 66px。
- 缩略图使用 8px 圆角、1px 中性边框和白色底板，保持原图比例。
- 缩略图悬停约 170ms 后显示 250px 级别的快速预览浮层，减少误触闪烁。
- 放大镜按钮使用 27×27 深色半透明风格，圆角 6px，置于缩略图右上角。
- 大图查看器以模态窗口呈现，主内容使用深色背景，工具栏与标题栏使用白色表面。
- 页面设置采用分组标题、细分隔线和小型矢量图标；输出路径固定在操作区。
- 进度条轨道高度 8px，百分比使用独立圆角胶囊，运行时显示轻微高光动画。
