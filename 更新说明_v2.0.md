# v2.0 产品与交互优化说明

## 拖拽成为主要入口

- PPT、图片和文件夹均可直接拖入整个窗口
- 自动识别文件类型并分发到对应工作区
- 自动去重、统计数量与总体积
- 导入单一类型文件时自动切换到对应功能
- 图片列表支持直接拖动行调整幻灯片页序

## 信息架构优化

- 左侧固定功能导航，两个转换任务不再混在页签中
- 顶部持续显示当前功能和导出引擎状态
- 中间采用“文件区 + 设置区”的双栏结构
- 底部固定任务状态与进度条
- 处理记录默认折叠，减少视觉干扰，需要时再展开

## 视觉优化

- 深色侧栏与浅色工作区形成明确层级
- 统一主色、间距、按钮、表格、空状态和反馈状态
- 拖拽区使用高识别度虚线边框和行动提示
- 重要动作使用蓝色主按钮，取消操作使用弱化危险样式
- 提供应用图标，打包 EXE 后可直接显示

## 效率优化

- PPT 导出和图片合成均提供常用预设
- 自动生成合理的默认输出路径
- 双击列表项可快速打开源文件目录
- 支持常用快捷键
- 窗口尺寸和上次使用设置自动保存

## 响应式处理

- 宽屏时显示图片预览
- 较矮窗口自动隐藏预览，优先保留关键设置与执行按钮
- 调整列宽与侧栏宽度，使 960px 宽度下仍可完成主要操作
